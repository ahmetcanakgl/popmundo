package co.aca.util;

public class LangString {

    public String getAdded(String locale) {
        if (locale.equals("tr"))
            return StringTR.ADDED;
        else
            return StringENG.ADDED;
    }

    public String getUpdated(String locale) {
        if (locale.equals("tr"))
            return StringTR.UPDATED;
        else
            return StringENG.UPDATED;
    }

    public String getDeleted(String locale) {
        if (locale.equals("tr"))
            return StringTR.DELETED;
        else
            return StringENG.DELETED;
    }

    public String getSuccess(String locale) {
        if (locale.equals("tr"))
            return StringTR.SUCCESS;
        else
            return StringENG.SUCCESS;
    }

    public String getApplied(String locale) {
        if (locale.equals("tr"))
            return StringTR.APPLIED;
        else
            return StringENG.APPLIED;
    }

    public String getRegistered(String locale) {
        if (locale.equals("tr"))
            return StringTR.REGISTERED;
        else
            return StringENG.REGISTERED;
    }

    public String getUnregistered(String locale) {
        if (locale.equals("tr"))
            return StringTR.UNREGISTERED;
        else
            return StringENG.UNREGISTERED;
    }

    public String getFillUsernamePassword(String locale) {
        if (locale.equals("tr"))
            return StringTR.FILL_USERNAME_PASSWORD;
        else
            return StringENG.FILL_USERNAME_PASSWORD;
    }

    public String getUsernameNotFound(String locale) {
        if (locale.equals("tr"))
            return StringTR.USERNAME_NOT_FOUND;
        else
            return StringENG.USERNAME_NOT_FOUND;
    }

    public String getInvalidLogin(String locale) {
        if (locale.equals("tr"))
            return StringTR.INVALID_LOGIN;
        else
            return StringENG.INVALID_LOGIN;
    }

    public String getLoginNotAllowed(String locale) {
        if (locale.equals("tr"))
            return StringTR.LOGIN_NOT_ALLOWED;
        else
            return StringENG.LOGIN_NOT_ALLOWED;
    }

    public String getWaitForConfirmation(String locale) {
        if (locale.equals("tr"))
            return StringTR.WAIT_FOR_CONFIRMATION;
        else
            return StringENG.WAIT_FOR_CONFIRMATION;
    }

    public String getUserExists(String locale) {
        if (locale.equals("tr"))
            return StringTR.USER_EXISTS;
        else
            return StringENG.USER_EXISTS;
    }

    public String getAdminExists(String locale) {
        if (locale.equals("tr"))
            return StringTR.ADMIN_EXISTS;
        else
            return StringENG.ADMIN_EXISTS;
    }

    public String getCantRegisterAgain(String locale) {
        if (locale.equals("tr"))
            return StringTR.CANT_REGISTER_AGAIN;
        else
            return StringENG.CANT_REGISTER_AGAIN;
    }

    public String getNoSuchACourse(String locale) {
        if (locale.equals("tr"))
            return StringTR.NO_SUCH_A_COURSE;
        else
            return StringENG.NO_SUCH_A_COURSE;
    }

    public String getAssigned(String locale) {
        if (locale.equals("tr"))
            return StringTR.ASSINGNED;
        else
            return StringENG.ASSINGNED;
    }

    public String getDismissed(String locale) {
        if (locale.equals("tr"))
            return StringTR.DISMISSED;
        else
            return StringENG.DISMISSED;
    }

    public String getCourseExists(String locale) {
        if (locale.equals("tr"))
            return StringTR.COURSE_EXISTS;
        else
            return StringENG.COURSE_EXISTS;
    }

    public String getAlreadyTaken(String locale) {
        if (locale.equals("tr"))
            return StringTR.ALREADY_TAKEN;
        else
            return StringENG.ALREADY_TAKEN;
    }

    public String getCourseCapasityFull(String locale) {
        if (locale.equals("tr"))
            return StringTR.COURSE_CAPASITY_FULL;
        else
            return StringENG.COURSE_CAPASITY_FULL;
    }

    public String getAddedToCourseList(String locale) {
        if (locale.equals("tr"))
            return StringTR.ADDED_TO_COURSE_LIST;
        else
            return StringENG.ADDED_TO_COURSE_LIST;
    }

    public String getNoSuchAnAdmin(String locale) {
        if (locale.equals("tr"))
            return StringTR.NO_SUCH_AN_ADMIN;
        else
            return StringENG.NO_SUCH_AN_ADMIN;
    }

    public String getAdminsAlreadyRegistered(String locale) {
        if (locale.equals("tr"))
            return StringTR.ADMINS_ALREADY_REGISTERED;
        else
            return StringENG.ADMINS_ALREADY_REGISTERED;
    }

    public String getAdminIsNotRegistered(String locale) {
        if (locale.equals("tr"))
            return StringTR.ADMIN_IS_NOT_REGISTERED;
        else
            return StringENG.ADMIN_IS_NOT_REGISTERED;
    }

    public String getYouRNotTakeThisCourse(String locale) {
        if (locale.equals("tr"))
            return StringTR.YOU_R_NOT_TAKE_THIS_COURSE;
        else
            return StringENG.YOU_R_NOT_TAKE_THIS_COURSE;
    }

    public String getAlreadyRegisteredThisCourse(String locale) {
        if (locale.equals("tr"))
            return StringTR.ALREADY_REGISTERED_THIS_COURSE;
        else
            return StringENG.ALREADY_REGISTERED_THIS_COURSE;
    }

    public String getNoSuchAnUser(String locale) {
        if (locale.equals("tr"))
            return StringTR.NO_SUCH_AN_USER;
        else
            return StringENG.NO_SUCH_AN_USER;
    }

    public String getUsersAlreadyRegistered(String locale) {
        if (locale.equals("tr"))
            return StringTR.USERS_ALREADY_REGISTERED;
        else
            return StringENG.USERS_ALREADY_REGISTERED;
    }

    public String getUserIsNotRegistered(String locale) {
        if (locale.equals("tr"))
            return StringTR.USER_IS_NOT_REGISTERED;
        else
            return StringENG.USER_IS_NOT_REGISTERED;
    }


    public String getNewUserRegister(String locale) {
        if (locale.equals("tr"))
            return StringTR.NEW_USER_REGISTER;
        else
            return StringENG.NEW_USER_REGISTER;
    }

    public String getNewAdminRegister(String locale) {
        if (locale.equals("tr"))
            return StringTR.NEW_ADMIN_REGISTER;
        else
            return StringENG.NEW_ADMIN_REGISTER;
    }

    public String getRegistrationIsDone(String locale) {
        if (locale.equals("tr"))
            return StringTR.REGISTRATION_IS_DONE;
        else
            return StringENG.REGISTRATION_IS_DONE;
    }

    public String getRegistrationIsDoneMsg(String locale) {
        if (locale.equals("tr"))
            return StringTR.REGISTRATION_IS_DONE_MSG;
        else
            return StringENG.REGISTRATION_IS_DONE_MSG;
    }

    public String getTakeCourseReq(String locale) {
        if (locale.equals("tr"))
            return StringTR.TAKE_COURSE_REQ;
        else
            return StringENG.TAKE_COURSE_REQ;
    }

    public String getTakeCourseReqIsDone(String locale) {
        if (locale.equals("tr"))
            return StringTR.TAKE_COURSE_REQ_IS_DONE;
        else
            return StringENG.TAKE_COURSE_REQ_IS_DONE;
    }

    public String getYouHavePrivateMessage(String locale) {
        if (locale.equals("tr"))
            return StringTR.YOU_HAVE_PRIVATE_MESSAGE;
        else
            return StringENG.YOU_HAVE_PRIVATE_MESSAGE;
    }
}
